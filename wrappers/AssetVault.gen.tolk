// Auto-generated wrapper for contract 'AssetVault'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@shared/messages"
import "@shared/storage"

/// Storage `AssetVaultStorage` is defined in `@shared/storage`
struct AssetVault {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun AssetVault.fromStorage(
    storage: AssetVaultStorage,
    toShard: AddressShardingOptions? = null,
): AssetVault {
    val stateInit = ContractState {
        code: build("AssetVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return AssetVault { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun AssetVault.fromAddress(address: address): AssetVault {
    return AssetVault { address };
}

/// Deploys the contract to the blockchain
fun AssetVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun AssetVault.sendDepositTon(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    l2Recipient: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DepositTon { queryId, amount, l2Recipient },
    });
    return net.send(from, genericMsg);
}

fun AssetVault.sendReleaseAuthorized(
    self,
    from: address,
    withdrawalId: uint256,
    assetId: uint32,
    recipient: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReleaseAuthorized { withdrawalId, assetId, recipient, amount },
    });
    return net.send(from, genericMsg);
}

fun AssetVault.sendRetryRelease(
    self,
    from: address,
    withdrawalId: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RetryRelease { withdrawalId },
    });
    return net.send(from, genericMsg);
}

fun AssetVault.sendJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun AssetVault.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun AssetVault.vaultStatus(self): VaultStatusReply {
    return net.runGetMethod(self.address, "vaultStatus");
}

fun AssetVault.failedRelease(self, withdrawalId: uint256): ReleaseFailureReply {
    return net.runGetMethod(self.address, "failedRelease", [withdrawalId]);
}
