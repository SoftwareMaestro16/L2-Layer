// Auto-generated wrapper for contract 'RollupRoot'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@shared/messages"
import "@shared/storage"

/// Storage `RollupStorage` is defined in `@shared/storage`
struct RollupRoot {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun RollupRoot.fromStorage(
    storage: RollupStorage,
    toShard: AddressShardingOptions? = null,
): RollupRoot {
    val stateInit = ContractState {
        code: build("RollupRoot"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return RollupRoot { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun RollupRoot.fromAddress(address: address): RollupRoot {
    return RollupRoot { address };
}

/// Deploys the contract to the blockchain
fun RollupRoot.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun RollupRoot.sendCommitBatch(
    self,
    from: address,
    batchNo: uint64,
    rootsA: BatchRootsA,
    rootsB: BatchRootsB,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CommitBatch {
            batchNo,
            rootsA: rootsA.toCell(),
            rootsB: rootsB.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun RollupRoot.sendFinalizeBatch(
    self,
    from: address,
    batchNo: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FinalizeBatch { batchNo },
    });
    return net.send(from, genericMsg);
}

fun RollupRoot.sendClaimWithdrawal(
    self,
    from: address,
    batchNo: uint64,
    withdrawalId: uint256,
    withdrawalLeaf: cell,
    merkleProof: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimWithdrawal { batchNo, withdrawalId, withdrawalLeaf, merkleProof },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun RollupRoot.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun RollupRoot.rollupStatus(self): RollupStatusReply {
    return net.runGetMethod(self.address, "rollupStatus");
}

fun RollupRoot.commitment(self, batchNo: uint64): CommitmentReply {
    return net.runGetMethod(self.address, "commitment", [batchNo]);
}
