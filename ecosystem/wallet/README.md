# EnWallet

Mock-only Entropis web wallet UI. This app does not generate seed phrases, derive
keys, sign payloads, or send blockchain transactions.

## Commands

```powershell
npm ci
npm run typecheck
npm run lint
npm run build
npm run dev
```

The wallet uses fixed mock account data and local in-memory state only.
